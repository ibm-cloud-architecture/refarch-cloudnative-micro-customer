apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: {{ .Chart.Name }}-couchdb
  labels:
{{ toYaml .Values.couchdb.labels | indent 4 }}
    release: {{.Release.Name | quote }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}"
spec:
  replicas: {{ .Values.replicaCount }}
  template:
    metadata:
      labels:
{{ toYaml .Values.couchdb.labels | indent 8 }}
        release: {{.Release.Name | quote }}
        chart: "{{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}"
    spec:
      containers:
      - name: bluecompute-customer-couchdb
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        env:
          - name: COUCHDB_USER
            valueFrom:
              secretKeyRef:
                name: {{ .Values.couchdb.binding.name }}
                key: user
          - name: COUCHDB_PASSWORD
            valueFrom:
              secretKeyRef:
                name: {{ .Values.couchdb.binding.name }}
                key: password
        volumeMounts:
          - mountPath: /opt/couchdb/data
            name: couchdb-data
            readOnly: false
        resources:
{{ toYaml .Values.couchdb.resources | indent 12 }}
        ports:
        - containerPort: {{ .Values.couchdb.service.internalPort }}
        livenessProbe:
          httpGet:
            path: /
            port: {{ .Values.couchdb.service.internalPort }}
          initialDelaySeconds: 30
        readinessProbe:
          httpGet:
            path: /
            port: {{ .Values.couchdb.service.internalPort }}
          initialDelaySeconds: 10
          failureThreshold: 10
      volumes:
        - name: couchdb-data
{{ if .Values.couchdb.existingPVCName }}
          persistentVolumeClaim:
            claimName: {{ .Values.couchdb.existingPVCName }}
{{ else }}
          hostPath:
            path: /opt/couchdb/data
{{ end }}            
